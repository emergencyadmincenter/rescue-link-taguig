{
  "name": "rescue-link-taguig-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "docker compose up -d db api",
    "dev:frontend": "npm run dev --prefix apps/web",
    "dev": "npm run dev:backend && npm run dev:frontend"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
